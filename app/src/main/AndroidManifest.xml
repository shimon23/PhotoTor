<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.phototor">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".searchPhotographersResults"></activity>
        <activity android:name=".searchPhotographer" />
        <activity android:name=".photographerProfileDisplay" />
        <activity android:name=".photographersList" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".activity_typeofwork" />
        <activity android:name=".activity_area" />
        <activity android:name=".clientMenu" />
        <activity android:name=".photographerMenu" />
        <activity android:name=".register" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>